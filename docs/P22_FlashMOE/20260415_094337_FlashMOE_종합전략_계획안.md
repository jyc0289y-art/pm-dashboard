# P22 FlashMOE 종합전략 계획안

> **세션**: P22.1wc (2026-04-15)
> **환경**: MacBook Pro M5 base / 32GB / 1TB SSD (523GB 여유)
> **전략 목표**:
> 1. 클라우드 LLM 수준 벤치마크의 로컬 LLM을 M5 맥북에 최적화
> 2. Claude보다 뛰어난 멀티모달 능력을 특화 모델로 보완하여 제공
> **버전**: v1.1 (P21 의견서 v2 피드백 반영)
> **변경 이력**:
> - v1.0 (09:43): 초안 작성
> - v1.1 (현재): P21 의견서 v2의 6건 피드백 반영 — 페이지 캐시 경합, SSD I/O 경합, 하이브리드 전략, 397B 폐기 기준 등

---

## 0. P21 의견서에 대한 반론

P21 의견서(`20260415_092844_P22_Flash_MOE_의견서.md`)는 **Flash-MoE SSD 스트리밍 기술을 전혀 모른 상태**에서 작성되었다. 이로 인해 "32GB에서 120B+ 불가"라는 결론에 도달했으나, 이는 **잘못된 전제에 기반한 오판**이다.

### Flash-MoE가 뒤집는 것

| P21 의견서 주장 | Flash-MoE 현실 |
|---|---|
| "32GB에서 120B+ MoE 구동 불가" | **122B, 397B 모두 구동 가능** (RAM 5.5GB만 사용, 나머지 SSD 스트리밍) |
| "60-70% 규칙: 모델은 RAM의 60-70% 이하" | Flash-MoE는 모델 크기와 무관하게 **~6GB RAM만 상주** |
| "MoE + ComfyUI 동시 운영 불가 (33GB > 32GB)" | Flash-MoE(6GB) + ComfyUI(15GB) = **21GB → 동시 가능** |
| "핫스왑이 유일한 해법" | Flash-MoE는 핫스왑 없이도 ComfyUI와 **동시 운영 가능** |
| "하드웨어 근본 해결은 128GB Mac 뿐" | 32GB에서도 Flash-MoE로 **GPT-5급 모델 구동 가능** |

### Flash-MoE 핵심 원리 (30초 요약)

```
[기존 방식]   모델 전체(209GB)를 RAM에 로드 → 32GB에서 불가능

[Flash-MoE]   비전문가 가중치(5.5GB)만 RAM에 상주
              토큰 1개 생성 시 → 512개 expert 중 4개만 SSD에서 실시간 로드
              각 expert ~5MB × 4개 × 60 layers = ~1.2GB/토큰 (SSD 읽기)
              SSD 6.3 GB/s + macOS 페이지 캐시(71% 히트) → 실용적 속도 달성
```

### P21 의견서에서 여전히 유효한 부분

- **토큰 절감 추정 (~44%)**: 방향성 유효, 122B 기준으로 절감률 더 높아질 수 있음
- **Worker 위임 확장 계획**: 위임 범위 확대 방향 그대로 채택
- **워크로드 스케줄링 자동화**: 핫스왑 코드 구조 참고 가능
- **Speculative Decoding**: Draft+Verifier 구조 유효

---

## 1. 하드웨어 스펙 (실측 기반)

| 항목 | 값 | 비고 |
|---|---|---|
| 칩 | Apple M5 (base, Mac17,2) | NOT Pro/Max |
| CPU | 10코어 (4P + 6E) | |
| GPU | 10코어, Metal 4 | |
| 메모리 | 32GB 통합 메모리 | |
| **메모리 대역폭** | **153 GB/s** | Max(614 GB/s)의 1/4 — 가장 중요한 제약 |
| SSD | APPLE SSD AP1024Z, 994.6GB | |
| SSD 여유 | **523GB** (2026-04-15 기준) | 정리 후 ~552GB |
| SSD 읽기 속도 | ~6.3 GB/s (추정) | M5 base 스펙 기반 |
| Tailscale | ✅ 활성 (100.105.171.93) | iPhone은 재연결 필요 |
| Ollama | v0.15.4 (localhost:11434) | 업그레이드 + 바인딩 변경 필요 |

### 기존 로컬 모델 현황 (총 ~47GB)

| 위치 | 크기 | 내용 | 사용 프로젝트 | 정리 가능? |
|---|---|---|---|---|
| ComfyUI/models | 29 GB | FLUX Kontext Dev (FP8+Q8), T5, CLIP, VAE | P21 FEP 활성 | **Q8 or FP8 중복 삭제 → 11-12GB 절약** |
| ~/.ollama | 13 GB | qwen2.5-coder:7b, qwen2.5:14b | **P21 FEP Worker 활성 (14B)** | ⚠️ **35B-A3B 교체 완료 후에만 삭제** (coder:7b는 즉시 삭제 가능 → 4.7GB 절약) |
| SadTalker | 2.5 GB | SadTalker V0.0.2 (256+512) | 미사용 | **전체 삭제 → 2.5GB 절약** |
| LivePortrait | 2.1 GB | base_models (human+animals) | P21 FEP 활성 | animals만 삭제 → 0.9GB |

**정리 순서**:
1. 즉시 삭제 가능: SadTalker(2.5GB) + qwen2.5-coder:7b(4.7GB) + FLUX 중복(11-12GB) + LP animals(0.9GB) = **~20GB**
2. 35B-A3B 교체 완료 후: qwen2.5:14b(9.0GB) 삭제 = **+9GB**
3. **정리 후 SSD 여유: ~543GB (1단계) → ~552GB (2단계)**

---

## 2. 모델 포트폴리오 전략

### 2-1. 텍스트 LLM (목표 1)

> **P21 오류 정정**: "30B-A3B가 상한"이 아니라, Flash-MoE로 122B/397B도 구동 가능

| 역할 | 모델 | 총 파라미터 | 활성 | 디스크 | RAM 사용 | 실행 방식 | 예상 속도 | 벤치마크 |
|---|---|---|---|---|---|---|---|---|
| **메인 추론** | Qwen3.5-122B-A10B | 122B | 10B | ~65GB (Q4) | **~6GB** | Flash-MoE | 5-10 tok/s | GPT-4o+급 |
| **빠른 응답** | Qwen3.5-35B-A3B | 35B | 3B | ~19GB (Q4) | ~19GB | RAM 적재 | 15-30 tok/s | GPT-4 mini급 |
| **최대 품질** | Qwen3.5-397B-A17B | 397B | 17B | ~150GB (Q3) | **~6GB** | Flash-MoE | 1.5-3 tok/s | GPT-5급 |

**왜 122B가 메인인가?**
- 397B (150GB Q3): 페이지 캐시 26GB로 캐시 히트율 ~17% → SSD I/O 병목 극심
- 122B (65GB Q4): 페이지 캐시 26GB로 캐시 히트율 **~40%** → 397B 대비 2배 이상 빠름
- 35B (19GB Q4): RAM 완전 적재 → 가장 빠르지만 벤치마크 낮음

### 2-2. 멀티모달 (목표 2) — Claude가 못하는 것만

| Claude 능력 | 한계 | 보완 모델 |
|---|---|---|
| 이미지 분석: 최강 (600장/요청) | 없음 | **보완 불필요** — Claude가 이미 최고 |
| **이미지 생성: SVG/코드만 가능** | 래스터 이미지 불가 | HunyuanImage-3.0 + FLUX 2.0 |
| **오디오 전사: 미지원** | 네이티브 미지원 | whisper-large-v3 |
| **비디오 이해: 미지원** | 네이티브 미지원 | Qwen3-VL-30B-A3B |

| 모달리티 | 모델 | 아키텍처 | 디스크 | RAM | Flash-MoE? |
|---|---|---|---|---|---|
| **이미지 생성 (최고 품질)** | HunyuanImage-3.0 | **MoE** (64 experts, 8 active) | ~42GB (Q4) | **~6GB** | **최적** — 87.5% 비활성, 오토리그레시브 |
| **이미지 생성 (빠른)** | FLUX 2.0 / Kontext Dev | Dense 12B | ~24GB (FP16) | ~24GB | 불가 (MoE 아님) |
| **이미지 생성 (스타일)** | SDXL + LoRA | Dense | ~7GB | ~7GB | 불가 |
| **오디오 전사** | whisper-large-v3 | Dense | ~3GB | ~3GB | 불가 (불필요) |
| **비디오 이해** | Qwen3-VL-30B-A3B | **MoE** | ~16GB (Q4) | ~6GB | **가능** |

**핵심 발견 — HunyuanImage-3.0:**
- 오픈소스 최대 이미지 생성 모델 (80B, 50억 이미지-텍스트 쌍)
- **MoE 아키텍처 + 오토리그레시브** = Flash-MoE가 텍스트 LLM과 동일하게 적용
- Flash-MoE로 구동 시 RAM 6GB → **ComfyUI FLUX와 동시 운영 가능**
- P21 FEP의 일러스트 품질을 크게 향상시킬 후보

### 2-3. SSD 용량 예산

| 모델 | 크기 | 누적 |
|---|---|---|
| Qwen3.5-397B-A17B (Q3) | 150 GB | 150 GB |
| Qwen3.5-122B-A10B (Q4) | 65 GB | 215 GB |
| Qwen3.5-35B-A3B (Q4) | 19 GB | 234 GB |
| HunyuanImage-3.0 (Q4) | 42 GB | 276 GB |
| FLUX 2.0 (FP16) | 24 GB | 300 GB |
| whisper-large-v3 | 3 GB | 303 GB |
| Qwen3-VL-30B-A3B (Q4) | 16 GB | 319 GB |
| **합계** | **319 GB** | 552GB 여유 중 319GB = **233GB 잔여** ✅ |

---

## 3. 성능 예상 (M5 base 153 GB/s 반영)

> **주의**: gorroai의 20.34 tok/s는 **M5 Max 128GB** (614 GB/s, 40코어) 기준.
> M5 base(153 GB/s, 10코어)는 대역폭 1/4, GPU 1/4이므로 직접 외삽 불가.

### 3-1. 텍스트 LLM

| 모델 | 설정 | 예상 속도 | 비고 |
|---|---|---|---|
| **122B (Q4)** | Flash-MoE + predict + cache-io-split | **5-10 tok/s** | 메인 추론, 실용적 |
| 35B (Q4) | RAM 적재 (Ollama+MLX) | **15-30 tok/s** | 빠른 응답용 |
| 397B (Q3) | Flash-MoE 풀 최적화 | **1.5-3 tok/s** | 최대 품질, 느림 |
| 397B (Q3) | + K=3 프루닝 | **2-4 tok/s** | 실험적 |

### 3-2. 동시 운영 메모리 예산

```
[Flash-MoE 모델] ← RAM ~6GB (비전문가 가중치만)
+ [ComfyUI FLUX]  ← RAM ~15GB (FP8 모델 로드)
+ [macOS]         ← RAM ~6GB
────────────────────────────
합계:              ~27GB / 32GB  → ✅ 동시 가능! (5GB 여유)
```

→ **P21 의견서 v1의 "동시 불가" 판정을 뒤집음.** Flash-MoE 모델은 RAM 6GB만 차지하므로 ComfyUI와 동시 운영이 가능하다.

### 3-3. ⚠️ 페이지 캐시 경합 (P21 v2 피드백 반영)

> **P21 지적**: 동시 운영은 메모리상 가능하나, **페이지 캐시 경합**으로 Flash-MoE 속도가 대폭 하락할 수 있다.

```
[Flash-MoE 단독]
사용 가능 페이지 캐시: 32GB - 6GB(macOS) - 6GB(Flash-MoE) = ~20GB
122B Q4 65GB 중 20GB 캐시 → 히트율 ~31%
→ 5-10 tok/s 달성 가능

[Flash-MoE + ComfyUI 동시]
사용 가능 페이지 캐시: 32GB - 6GB(macOS) - 6GB(Flash-MoE) - 15GB(ComfyUI) = ~5GB
122B Q4 65GB 중 5GB 캐시 → 히트율 ~8%
→ 거의 모든 expert를 SSD에서 읽어야 함 → 속도 대폭 저하 (2-4 tok/s 추정)
```

### 3-4. 하이브리드 운영 전략 (P21 v2 제안 채택)

페이지 캐시 경합 문제에 대한 대응:

```
[이미지 생성 Phase] — ComfyUI 활성 상태
  └── LLM: 35B-A3B RAM 적재 (19GB) 사용
      → ComfyUI(15GB) + 35B(19GB) + macOS(6GB) = 40GB → ❌ 초과
      → 대안: ComfyUI 생성 완료 대기 후 35B 사용, 또는 Flash-MoE 저속(2-4 tok/s) 감수

[비이미지 Phase] — ComfyUI 비활성
  └── LLM: 122B Flash-MoE (6GB) 사용
      → 캐시 ~20GB → 히트율 ~31% → 5-10 tok/s

[결론]
  · ComfyUI 이미지 생성 중 LLM 동시 사용: Flash-MoE 122B (저속 2-4 tok/s 감수)
  · ComfyUI 미사용 시: Flash-MoE 122B (정상 속도 5-10 tok/s)
  · 인터랙티브 빠른 응답 필요 시: ComfyUI 종료 후 35B RAM 적재 (15-30 tok/s)
```

### 3-5. ⚠️ SSD I/O 경합 (P21 v2 피드백 반영)

Flash-MoE의 SSD 스트리밍과 ComfyUI의 SSD 접근이 동시 발생 시:
- Flash-MoE: 토큰당 ~1.2GB SSD 읽기
- ComfyUI: 모델 로드 시 ~15GB + 샘플링 중 간헐적 접근
- **SSD 6.3 GB/s 대역폭을 분할** → Flash-MoE 속도 추가 저하 가능

→ **Phase 2에서 `iostat` 벤치마크로 실측 필수**

---

## 4. Claude 연동 아키텍처

### 4-1. 전체 구조

```
[Claude Code / Claude Desktop]
       │ MCP Protocol
       ▼
[FlashMOE MCP Server] (Python, localhost:5050)
  ├── /text        → Ollama API → Qwen3.5-122B (Flash-MoE) or 35B (RAM)
  ├── /image-gen   → ComfyUI API → FLUX 2.0 (빠른) or HunyuanImage (품질)
  ├── /audio       → Whisper API → whisper-large-v3
  └── /video       → MLX-VLM API → Qwen3-VL-30B-A3B
```

### 4-2. Worker 위임 확장

현재 Ollama Worker (7B/14B) → **Qwen3.5-122B Flash-MoE로 교체**
- 기존 위임: 요약/추출 (사실 확인)
- 확장 위임: + 구조화 생성(JSON/manifest), 1차 코드 리뷰, 번역, 파일 분석/파싱
- 예상 API 토큰 절감: **~55%** (P21 v2 재추정 채택 — v1의 44%에서 상향, 122B 위임 범위 확대 반영)

### 4-3. 라우팅 미들웨어

```
[작업 요청] → [복잡도 판단기]
  ├── 단순 (토큰 <2K, 구조화 출력) → 로컬 MoE
  ├── 중간 (코드 리뷰, 번역)        → 로컬 MoE + 10% 품질 샘플링
  └── 복잡 (아키텍처 설계, 각본)     → Claude API
```

---

## 5. 독립 UI + 아이폰 접근

### 5-1. 네이티브 데스크톱 앱 — "FlashMOE" (SwiftUI)

> **방향**: Open WebUI 같은 웹 UI가 아닌, **Claude Desktop 수준의 독립 네이티브 앱**을 직접 구축한다.
> SwiftUI 기반으로 macOS + iOS 코드베이스를 공유하여 두 플랫폼을 동시 커버.

#### 앱 개요

| 항목 | 상세 |
|---|---|
| **앱 이름** | FlashMOE (또는 사용자 결정) |
| **프레임워크** | SwiftUI (macOS 15+ / iOS 18+) |
| **아키텍처** | MVVM + Swift Concurrency (async/await) |
| **백엔드 연결** | Ollama REST API (`localhost:11434`), ComfyUI API (`localhost:8188`) |
| **데이터 저장** | SwiftData (대화 기록, 설정) + CloudKit 동기화 (Mac↔iPhone) |
| **배포** | 로컬 빌드 (Xcode), 개인 사용 → App Store 불필요 |

#### 핵심 화면 구성

```
┌──────────────────────────────────────────────────────────────┐
│  FlashMOE                                        ─ □ ✕     │
├──────────┬───────────────────────────────────────────────────┤
│          │                                                   │
│ 대화 목록  │  ┌─────────────────────────────────────────────┐ │
│          │  │  [122B Flash-MoE ▾] ← 모델 선택 드롭다운     │ │
│ □ 새 대화  │  ├─────────────────────────────────────────────┤ │
│ ▸ 어제    │  │                                             │ │
│   코드리뷰 │  │  사용자: 이 코드 분석해줘                     │ │
│   번역작업 │  │                                             │ │
│ ▸ 지난주  │  │  FlashMOE: 이 코드는 MoE 라우터의 핵심       │ │
│   아키텍처 │  │  부분으로, top-K expert 선택 로직을...        │ │
│          │  │                                             │ │
│──────────│  │  [이미지 첨부] [파일 첨부] [코드 블록]         │ │
│          │  ├─────────────────────────────────────────────┤ │
│ ⚡ 시스템   │  │  메시지 입력...                    [전송 ▶]  │ │
│ RAM 21/32 │  └─────────────────────────────────────────────┘ │
│ SSD 2.1/s │                                                  │
│ ● 122B    │                                                  │
│ ● FLUX    │                                                  │
│ ○ Whisper │                                                  │
└──────────┴───────────────────────────────────────────────────┘
```

#### 기능 상세

| 기능 | 설명 |
|---|---|
| **채팅 인터페이스** | Claude Desktop급 마크다운 렌더링, 코드 하이라이트(swift-markdown), LaTeX |
| **모델 핫스왑** | 상단 드롭다운에서 122B↔35B↔397B 즉시 전환 (Ollama `/api/pull`, `/api/generate`) |
| **스트리밍 응답** | Ollama streaming API (`/api/chat` stream:true) → 토큰 단위 실시간 표시 |
| **대화 기록** | SwiftData로 로컬 영구 저장 + CloudKit으로 iPhone과 자동 동기화 |
| **이미지 생성** | ComfyUI API 연동 — 프롬프트 입력 → 생성 진행률 표시 → 결과 인라인 표시 |
| **이미지 첨부** | VLM 모드 (Qwen3-VL) — 이미지 드래그&드롭 → base64 → Ollama vision API |
| **오디오 전사** | Whisper 연동 — 음성 파일 드래그&드롭 → 전사 결과 채팅에 삽입 |
| **시스템 모니터** | 사이드바 하단에 RAM/SSD/모델 상태 실시간 표시 (`sysctl`, `vm_stat`) |
| **TRAP 뷰어** | production_lessons.md 파싱 → TRAP 항목 목록, 검색, 필터 |
| **키보드 단축키** | ⌘N 새대화, ⌘1/2/3 모델전환, ⌘Enter 전송 (Claude Desktop 유사) |

#### 모델 관리 패널 (⌘, 설정)

```
┌─────────────────────────────────────────────────┐
│  모델 관리                                        │
├─────────────────────────────────────────────────┤
│                                                  │
│  [텍스트 모델]                                    │
│  ├── Qwen3.5-122B-A10B  ● 활성  [RAM 6GB]  ⟳    │
│  ├── Qwen3.5-35B-A3B    ○ 대기  [RAM 19GB] ▶    │
│  └── Qwen3.5-397B-A17B  ○ 대기  [RAM 6GB]  ▶    │
│                                                  │
│  [멀티모달]                                       │
│  ├── FLUX Kontext Dev    ● 활성  [RAM 15GB] ⟳    │
│  ├── HunyuanImage-3.0   ○ 대기  [SSD 42GB] ▶    │
│  ├── whisper-large-v3    ○ 대기  [RAM 3GB]  ▶    │
│  └── Qwen3-VL-30B-A3B   ○ 대기  [RAM 16GB] ▶    │
│                                                  │
│  [시스템 자원]                                     │
│  메모리  ████████████████░░░░░░ 21/32 GB          │
│  SSD I/O ██████░░░░░░░░░░░░░░ 2.1/6.3 GB/s      │
│  캐시    █████░░░░░░░░░░░░░░░░ 5 GB 활성          │
│                                                  │
│  [TRAP 로그] (최근 5건)                            │
│  TRAP-031: Flash-MoE + ComfyUI 동시 시 캐시 경합  │
│  TRAP-030: ...                                   │
│  → production_lessons.md 전체 보기                 │
└─────────────────────────────────────────────────┘
```

#### 기술 스택

| 레이어 | 기술 | 역할 |
|---|---|---|
| UI | SwiftUI + swift-markdown | 채팅 렌더링, 코드 하이라이트 |
| 네트워킹 | URLSession + AsyncBytes | Ollama 스트리밍, ComfyUI WebSocket |
| 데이터 | SwiftData + CloudKit | 대화 기록, 설정, Mac↔iPhone 동기화 |
| 시스템 모니터 | IOKit + sysctl | RAM/GPU/SSD 실시간 상태 |
| 이미지 처리 | Vision + CoreImage | 첨부 이미지 전처리, 생성 이미지 표시 |
| 오디오 | AVFoundation | 녹음 → Whisper 전사 |

#### macOS ↔ iOS 공유 전략

| 공유 대상 | 방식 |
|---|---|
| 모델 레이어 (API 통신, 데이터) | Swift Package — 100% 공유 |
| 채팅 뷰 | SwiftUI — 90% 공유 (레이아웃 분기만) |
| 시스템 모니터 | macOS 전용 (iOS에서는 서버 상태 원격 조회) |
| 키보드 단축키 | macOS 전용 |
| 사이드바 | macOS: NavigationSplitView / iOS: NavigationStack |

#### Open WebUI 대비 장점

| 비교 | Open WebUI (웹) | FlashMOE 네이티브 앱 |
|---|---|---|
| 실행 | 브라우저 탭 | **독립 앱 (Dock 상주)** |
| 시스템 통합 | 없음 | **메모리/GPU 모니터, 알림 센터** |
| 오프라인 | 불가 | **대화 기록 로컬 저장** |
| iOS 코드 공유 | 불가 | **SwiftUI 90% 공유** |
| 키보드 단축키 | 브라우저 제한 | **네이티브 단축키** |
| 성능 | 웹 렌더링 오버헤드 | **Metal 가속 렌더링** |
| CloudKit 동기화 | 불가 | **Mac↔iPhone 대화 자동 동기화** |

#### 이미지 생성 UI: ComfyUI 연동 (기존 유지)

| 항목 | 상세 |
|---|---|
| **역할** | FLUX / HunyuanImage 이미지 생성 백엔드 |
| **연동 방식** | FlashMOE 앱 내에서 ComfyUI API (`localhost:8188`)로 프롬프트 전송 |
| **워크플로우** | 채팅에서 `/image 프롬프트` 입력 → ComfyUI 큐 등록 → 진행률 → 결과 인라인 |
| **P21 FEP 호환** | 기존 ComfyUI 웹 UI도 병행 사용 가능 (독립 포트) |

### 5-2. iPhone 접근 — Tailscale 없이

> **사용자 요구**: iPhone에서 Tailscale은 불편하므로 사용하지 않는다.
> Tailscale은 MacBook↔iPad 간에만 유지하고, iPhone은 별도 방법으로 접근한다.

#### 권장: Cloudflare Tunnel (무료, 앱 불필요)

| 항목 | Cloudflare Tunnel | Tailscale (참고) |
|---|---|---|
| iPhone 앱 필요? | **불필요** — Safari만으로 접근 | Tailscale 앱 상시 실행 필요 |
| 설정 위치 | **MacBook에서만** (cloudflared 설치) | 양쪽 모두 |
| 프로토콜 | **HTTPS** (자동 SSL 인증서) | WireGuard VPN |
| 외부 네트워크 | **어디서든 접근 가능** (카페, LTE 등) | VPN 터널 필요 |
| 보안 | Cloudflare Access (이메일 인증/OTP) | Private 네트워크 |
| 비용 | **무료** | 무료 |
| 속도 | Cloudflare CDN 경유 (~20-50ms 추가) | P2P 직접 연결 |

**구성:**
```bash
# MacBook에 cloudflared 설치
brew install cloudflared

# 터널 생성 (1회)
cloudflared tunnel create flashmoe

# 서비스 노출
cloudflared tunnel route dns flashmoe flashmoe.yourdomain.com

# config.yml
tunnel: flashmoe
ingress:
  - hostname: api.yourdomain.com     # FlashMOE 백엔드 API
    service: http://localhost:5050
  - hostname: comfy.yourdomain.com   # ComfyUI
    service: http://localhost:8188
  - service: http_status:404
```

**iPhone FlashMOE 앱에서:**
- 앱 설정에 `api.yourdomain.com` 입력 → HTTPS로 MacBook Ollama에 접근
- Tailscale 앱 없이 어디서든 접근 가능
- Cloudflare Access로 이메일 인증 보안 추가 가능

#### 대안 비교

| 방법 | 앱 필요 | 외부 접근 | 보안 | 설정 난이도 |
|---|---|---|---|---|
| **Cloudflare Tunnel** | ❌ | ✅ 어디서든 | HTTPS + Access | 중간 (권장) |
| ngrok | ❌ | ✅ | HTTPS | 쉬움 (무료 제한) |
| 공유기 포트포워딩 + DDNS | ❌ | ✅ | 낮음 (직접 노출) | 어려움 |
| 같은 Wi-Fi만 | ❌ | ❌ 집에서만 | 높음 (LAN) | 쉬움 |

### 5-3. iOS 앱 — macOS 앱과 코드베이스 공유

> macOS FlashMOE 앱과 **동일 Xcode 프로젝트** (멀티플랫폼 타겟)로 구축.
> SwiftUI + SwiftData + CloudKit 공유로 개발 비용 최소화.

| 항목 | 상세 |
|---|---|
| **공유율** | 모델/네트워킹/데이터 레이어 100%, UI 레이어 ~85% 공유 |
| **접속 방식** | Cloudflare Tunnel HTTPS → MacBook Ollama/ComfyUI |
| **대화 동기화** | CloudKit — Mac에서 한 대화를 iPhone에서 이어서 열람 |
| **오프라인** | 최근 대화 SwiftData 로컬 캐시 (서버 미접속 시 읽기 가능) |
| **알림** | 이미지 생성 완료, 모델 로드 완료 → UserNotifications |
| **TRAP 뷰어** | production_lessons.md 파싱 → 모바일에서 TRAP 항목 검색/열람 |
| **iOS 전용 UI** | NavigationStack, 컴팩트 사이드바, 햅틱 피드백 |
| **개발 추가분** | macOS 앱 완성 후 iOS 타겟 추가 ~3-5일 (레이아웃 분기 작업) |

### 5-4. 상시 서버 운영 체크리스트

| 항목 | 현재 | 필요 조치 | 난이도 |
|---|---|---|---|
| Ollama | localhost만 | `OLLAMA_HOST=0.0.0.0` | 쉬움 |
| 슬립 방지 | 미설정 | `caffeinate -di` 또는 Amphetamine | 쉬움 |
| 자동 시작 (Ollama) | 미설정 | launchd plist | 쉬움 |
| FlashMOE 백엔드 API | 미구축 | Python/Swift 서버 + launchd | 중간 |
| **Cloudflare Tunnel** | 미설치 | `brew install cloudflared` + 터널 설정 | 중간 |
| 전원 | — | 어댑터 상시 연결 권장 | — |
| 보안 | — | Cloudflare Access (이메일 OTP) | 중간 |
| Tailscale | ✅ 활성 | MacBook↔iPad 간 유지, **iPhone에서는 사용 안 함** | — |

### 5-5. TRAP 시스템 적용

TRAP 시스템은 전역지침(`~/.claude/CLAUDE.md` 35-41행)에 이미 등록되어 있다.

**P22 FlashMOE에서의 TRAP 적용:**
- **기록 위치**: `FlashMOE/docs/production_lessons.md`
- **대시보드 연동**: 최근 TRAP 항목을 대시보드에 실시간 표시
- **iOS 앱 연동**: TRAP 뷰어 탭에서 모든 프로젝트의 production_lessons.md 열람
- **자동 감지**: Phase 2 벤치마크에서 예상 밖 결과 발생 시 TRAP 자동 등록 권고

---

## 6. 구현 로드맵

### Phase 0: 환경 정리 + 모델 선택 비교 (Day 1)

| 시간 | 작업 | 상세 |
|---|---|---|
| 0:00-0:30 | SSD 정리 | Ollama 7B/14B 삭제, SadTalker 삭제, FLUX 중복 확인 후 삭제 |
| 0:30-1:00 | Ollama 업그레이드 | v0.15.4 → 0.19+ (MLX 백엔드), OLLAMA_HOST=0.0.0.0 |
| 1:00-2:00 | Qwen3.5-35B-A3B 다운로드 + 테스트 | ~19GB, RAM 적재, 속도/품질 벤치마크 |
| 2:00-4:00 | 35B vs 기존 14B 비교 | 같은 작업으로 품질/속도 비교 |

### Phase 1: Flash-MoE 기본 구동 (Day 2-3)

| 시간 | 작업 | 상세 |
|---|---|---|
| 0:00-2:00 | gorroai/flash-moe 클론 + 빌드 | C/Metal 컴파일, 의존성 확인 |
| 2:00-6:00 | Qwen3.5-122B-A10B 다운로드 | ~65GB (Q4 GGUF), 네트워크 속도 의존 |
| 6:00-8:00 | 기본 실행 테스트 | `--q3-experts --predict --cache-io-split 4` |
| 8:00-10:00 | 성능 측정 | `--timing`, per-layer 속도, 페이지 캐시 히트율 |

### Phase 2: 32GB 최적화 (Day 4-5)

| 시간 | 작업 | 상세 |
|---|---|---|
| 0:00-2:00 | 컨텍스트 길이 튜닝 | 2K/4K/8K 비교, 페이지 캐시 영향 측정 |
| 2:00-4:00 | K=3 프루닝 테스트 | 속도 향상 vs 품질 저하 측정 |
| 4:00-6:00 | **ComfyUI 동시 운영 + 경합 벤치마크** | Flash-MoE(6GB) + ComfyUI(15GB) 동시 실행, **`iostat`로 SSD I/O 경합 실측**, 페이지 캐시 히트율 비교 (단독 vs 동시) |
| 6:00-8:00 | 벤치마크 | MMLU, GPQA 서브셋으로 품질 확인 |

> **Phase 2 판정 기준 (P21 v2 피드백 반영)**:
> - ComfyUI 동시 운영 시 Flash-MoE 속도가 단독 대비 50% 이상 하락 → 하이브리드 전략 채택 (이미지 Phase에서는 35B 사용)
> - 50% 미만 하락 → 동시 운영 유지

### Phase 3: 네이티브 앱 + 서버 인프라 (Day 6-10)

| 일차 | 작업 | 상세 |
|---|---|---|
| Day 6 | Xcode 프로젝트 생성 | SwiftUI 멀티플랫폼 (macOS + iOS), MVVM 구조, SwiftData 스키마 |
| Day 7 | 채팅 UI 핵심 | Ollama `/api/chat` 스트리밍 연동, 마크다운 렌더링, 모델 선택 드롭다운 |
| Day 8 | 대화 관리 + 시스템 모니터 | SwiftData 영구 저장, 사이드바 대화 목록, RAM/SSD 모니터 (IOKit) |
| Day 9 | 서버 인프라 | Ollama OLLAMA_HOST=0.0.0.0, Cloudflare Tunnel 설치, launchd 자동시작, 슬립 방지 |
| Day 10 | iOS 타겟 + 원격 테스트 | iOS 타겟 추가, Cloudflare Tunnel 경유 iPhone 테스트, CloudKit 동기화 |

### Phase 4: 멀티모달 MCP 서버 (Week 2-3)

| 일차 | 작업 | 상세 |
|---|---|---|
| Day 11-12 | MCP 서버 스캐폴딩 | Python MCP 서버 (localhost:5050에 이미 뭔가 있음 — 확인 필요) |
| Day 13 | /text 엔드포인트 | Ollama API 연동, 모델 선택 로직 |
| Day 14 | /image-gen 엔드포인트 | ComfyUI API 연동, **P21 워크플로우 JSON 호환 필수** (UnetLoaderGGUF 노드 인지) |
| Day 15 | /audio 엔드포인트 | whisper-large-v3 설치 + API, **Typecast TTS 출력물 자동 QA 유스케이스 포함** |
| Day 16 | /video 엔드포인트 | Qwen3-VL-30B-A3B 다운로드 + MLX-VLM |
| Day 17 | Claude Code 통합 테스트 | MCP 설정, 도구 호출 테스트 |
| Day 18 | FlashMOE 앱 ↔ MCP 통합 | 앱 내에서 이미지 생성/오디오 전사 UI 연결 |

### Phase 5: HunyuanImage-3.0 Flash-MoE (Week 3-4)

| 일차 | 작업 | 상세 |
|---|---|---|
| Day 19-20 | HunyuanImage-3.0 다운로드 | ~42GB (Q4), Flash-MoE 호환 포맷 확인 |
| Day 21-22 | Flash-MoE 이미지 생성 실험 | 텍스트 LLM용 Flash-MoE를 이미지 생성에 적용 |
| Day 23-24 | FLUX vs HunyuanImage A/B 테스트 | **P21 제공 프롬프트 셋 사용** (로케이션 8장 + 캐릭터 24종), 일러스트 품질/속도/스타일 비교 |
| Day 25 | P21 FEP 통합 테스트 | FEP 파이프라인에 HunyuanImage 적용, EP.02 적용 여부 결정 |

### Phase 6: 고급 최적화 + 앱 완성 (Week 4)

| 일차 | 작업 | 상세 |
|---|---|---|
| Day 26-27 | 397B 다운로드 + 테스트 | ~150GB (Q3), 최대 품질 모드 검증. **폐기 기준: 1.5 tok/s 미만 시 삭제하고 122B에 집중 → 150GB SSD 절약** |
| Day 28-29 | Worker 위임 규칙 확장 | worker_rules.md 업데이트, 라우팅 미들웨어 |
| Day 30 | FlashMOE 앱 마무리 | TRAP 뷰어, 모델 관리 패널, 이미지 생성 인라인 표시, ComfyUI 연동 완성 |
| Day 31 | iOS 앱 최종 테스트 + 배포 | TestFlight 또는 Xcode 직접 설치, CloudKit 동기화 검증 |

---

## 7. Flash-MoE 멀티모달 적용성

### Flash-MoE가 적용 가능한 조건

Flash-MoE는 **MoE 아키텍처(sparse experts)인 모델에만 적용 가능**하다. Dense 모델에는 적용 불가.

| 모델 | MoE 여부 | Flash-MoE 적합도 | 이유 |
|---|---|---|---|
| Qwen3.5 텍스트 LLM (122B/397B) | MoE (512 experts) | **최적** | 99.2% 비활성, 검증됨 |
| **HunyuanImage-3.0** | **MoE (64 experts)** | **최적** | 87.5% 비활성, 오토리그레시브 = 텍스트 LLM과 동일 |
| Qwen3-VL-30B-A3B | MoE | **양호** | 모달리티별 라우팅 차이 주의 |
| FLUX / Stable Diffusion | **Dense** | **불가** | MoE 아님, 전체 파라미터 활성 |
| Whisper | **Dense** | **불가** | MoE 아님, 3GB로 RAM 충분 |

### HunyuanImage-3.0이 특별한 이유

전통 디퓨전 모델(FLUX, SD)은 20-50 디노이징 스텝을 반복하며 모든 파라미터를 활성화한다. MoE가 아니므로 Flash-MoE 적용 불가.

HunyuanImage-3.0은:
1. **MoE 아키텍처**: 64 experts 중 8개만 활성 (87.5% 비활성)
2. **오토리그레시브 방식**: 디퓨전이 아닌 토큰 단위 생성 → Flash-MoE 프리페치 그대로 적용
3. **80B 파라미터**: 50억 이미지-텍스트 쌍으로 학습한 오픈소스 최대 이미지 생성 모델
4. **RAM 6GB**: Flash-MoE로 비전문가 가중치만 상주 → ComfyUI FLUX와 동시 운영 가능

**현재 멀티모달 Flash-MoE 구현체는 존재하지 않음** (2026-04 기준) → 선구적 연구 기회

---

## 8. P21 협업 요구사항 (의견서 v2 반영)

P21이 P22에 요청한 4건과 P22의 대응:

| # | P21 요청 | P22 대응 | Phase |
|---|---|---|---|
| 1 | **Worker 위임 인터페이스 표준화** — P21 `worker_rules.md`와 호환, MCP 전환 시 파이프라인 무중단 | MCP `/text` 엔드포인트가 기존 Ollama CLI 호출과 동일 결과 반환하도록 설계. 전환 기간에 MCP + 기존 방식 병행 | Phase 4 |
| 2 | **ComfyUI MCP 호환** — P21 워크플로우 JSON(UnetLoaderGGUF 노드) 그대로 전달 | `/image-gen` MCP 래퍼가 ComfyUI prompt JSON을 투명하게 전달. 커스텀 노드 인지 | Phase 4 |
| 3 | **HunyuanImage A/B 테스트** — P21 실제 씬 프롬프트로 FLUX vs HunyuanImage 비교 | P21이 프롬프트 셋(로케이션 8장 + 캐릭터 24종) 제공 → Phase 5 Day 19-20에서 실행 | Phase 5 |
| 4 | **동시 운영 검증** — ComfyUI + Flash-MoE 동시 시 P21 순차 패턴 개선 가능 여부 | Phase 2 Day 4-5에서 SSD I/O + 페이지 캐시 벤치마크 실측 후 결정 | Phase 2 |

### P21 → P22 도입 타임라인

```
Phase 0-1 완료 (Day 3) → P21 즉시 적용 가능:
  ① Ollama Worker 35B-A3B 교체 (품질 2-3x 향상)
  ② OLLAMA_HOST=0.0.0.0 (iPhone에서 Worker 상태 확인)
  ③ FlashMOE 앱 (모바일에서 각본 검토 — iOS 앱 or Cloudflare 경유)

Phase 2 완료 (Day 5) → P21 적용 가능:
  ④ Flash-MoE 122B + ComfyUI 동시 운영 여부 확정
  ⑤ 하이브리드 전략 적용 여부 결정

Phase 4 완료 (Day 14) → P21 적용 가능:
  ⑥ MCP /text 엔드포인트 → Claude Code에서 투명 호출
  ⑦ MCP /image-gen → P21 generate_scenes.py 연동
  ⑧ 라우팅 미들웨어 → 자동 로컬/API 분배

Phase 5 완료 (Day 21) → P21 검토:
  ⑨ HunyuanImage vs FLUX 결과 → EP.02부터 적용 여부 결정
  ⑩ Whisper → Typecast TTS 자동 QA
```

---

## 9. 핵심 수치 정리

### 메모리 대역폭 비교

| 칩 | 메모리 대역폭 | GPU 코어 | 비고 |
|---|---|---|---|
| **M5 base (사용자)** | **153 GB/s** | 10코어 | |
| M5 Max 32코어 | 460 GB/s | 32코어 | |
| M5 Max 40코어 | 614 GB/s | 40코어 | gorroai 20.34 tok/s 환경 |

→ gorroai 벤치마크를 M5 base에 직접 적용하면 안 됨 (대역폭/GPU 모두 1/4)

### SSD 내구성

Flash-MoE는 **읽기 전용** (쓰기 없음) → SSD 마모 무관. 걱정 불필요.

---

## 9. 참고 자료

| 자료 | URL |
|---|---|
| 원본 Flash-MoE | https://github.com/danveloper/flash-moe |
| gorroai 포크 (핵심 레퍼런스) | https://github.com/gorroai/flash-moe |
| FlashTier-MoE (핫스왑) | https://github.com/w3t-wr3/FlashTier-MoE |
| FlashMoE 논문 (ML 캐시) | https://arxiv.org/abs/2601.17063 |
| HunyuanImage-3.0 | https://arxiv.org/html/2509.23951v1 |
| MLX-VLM | https://github.com/Blaizzy/mlx-vlm |
| swift-markdown | https://github.com/apple/swift-markdown |
| Apple M5 스펙 | https://en.wikipedia.org/wiki/Apple_M5 |
| Unsloth Qwen3.5 GGUF | https://huggingface.co/unsloth/Qwen3.5-397B-A17B-GGUF |

---

*작성: P22.1wc (2026-04-15)*
*v1.1 업데이트: P21 의견서 v2 피드백 6건 반영*
*환경: MacBook Pro 14" M5 base / 32GB / 1TB SSD*
