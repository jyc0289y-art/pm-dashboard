# P18.6wc — 보안 수정 + Streamlit 알림 UI + 용어 통일

## 메타데이터
- 생성: 2026-04-11
- 포크원: P18.5wc
- PM 파견: PM.12wc

## 작업 요약 (3~5줄)
1. DB 비밀번호를 config.yaml에서 환경변수 플레이스홀더로 변경, config.py/database.py에서 `os.environ.get('HIGHLINK_DB_PASSWORD', 'highlink_dev')` 패턴 적용
2. Streamlit 알림 UI 신규 구현: `event_panel.py` 모듈 + `app.py` 통합. 사이드바 요약 + 메인 이벤트 목록 + 심각도별 색상 + 필터링
3. 용어 통일 확인: 코드베이스 전체 "event" 통일 이미 완료. `max_alerts` -> `max_events` 1건 수정, `use_incident_api`는 외부 API 명칭이므로 주석 보완

## 주요 결과물

| 파일 | 변경 내용 |
|------|----------|
| `engine/config.yaml` | password 플레이스홀더화, max_alerts→max_events, incident_api 주석 보완 |
| `engine/core/config.py` | ${} 플레이스홀더 감지 + 기본값 fallback 로직 추가 |
| `engine/core/database.py` | init_db()에서 HIGHLINK_DB_PASSWORD 환경변수 사용 |
| `app/streamlit/event_panel.py` | **신규** — 이벤트 저장소, 사이드바 요약, 메인 패널 UI |
| `app/streamlit/app.py` | event_panel 임포트, check_and_emit_events 함수, 양 모드에 이벤트 감지 통합 |

## 핵심 결정사항
- DB 비밀번호: YAML에 `${HIGHLINK_DB_PASSWORD}` 플레이스홀더 → config.py에서 환경변수 우선, 없으면 기본값 `highlink_dev`
- 이벤트 알림: session_state 기반 인메모리 저장 (Phase 1 PoC에 적합)
- 중복 억제: 같은 카메라+유형은 10초 내 중복 발생 방지
- 용어: "event" 통일 확인 완료, 외부 API 명칭(incident_api)은 유지

## 다음 작업을 위한 컨텍스트
- REQ-002 (실전 CCTV 테스트) 미착수 — 이벤트 알림 UI 완성으로 테스트 준비 완료
- REQ-005 (F4 정체/사고 감지) 미착수 — event_panel이 congestion/accident 유형 이미 지원
- 이벤트 알림이 실제 CCTV 스트림에서 제대로 동작하는지 회사 PC에서 검증 필요
