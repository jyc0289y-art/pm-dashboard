# P18.4wc — MJPEG 원격 임베딩 이슈 해결 + F3 테스트

## 메타데이터
- 생성: 2026-04-10
- 포크원: P18.3wc
- 자식: (없음)

## 작업 요약
P18.3wc에서 포크. 회사 PC 브랜치 `fix/remote-frame-drop` 머지 완료 (MJPEG 아키텍처 전환, FPS/품질 슬라이더, 오토모드, ThreadingMixIn 버그 수정). 이후 회사 PC에서 추가 푸시한 MJPEG 원격 임베딩 이슈 분석 — Streamlit(8501)과 MJPEG(8502)가 별도 Cloudflare 터널 도메인을 받아 cross-origin 차단되는 문제. 4가지 해결 방안 중 Solution 1 (Single Port 통합) 추천, 방향 결정 대기 중.

## 주요 결과물
- 머지: `fix/remote-frame-drop` → `main` (충돌 없음, 8건 커밋)
- 풀: 회사 PC 추가 커밋 8건 (ThreadingMixIn, /viewer, /snapshot, 터널 URL 자동화 등)
- `docs/requests/20260410_215500_P18_w3wc_remote_MJPEG_issue.md` — 이슈 인수인계 문서

## 핵심 결정사항
1. **MJPEG 원격 임베딩 근본 원인**: 2포트 → 2터널 → cross-origin 차단
2. **회사 PC 수정 유효분**: ThreadingMixIn, /viewer, /snapshot, 터널 URL 자동화
3. **해결 방안 우선순위**: Solution 1 (Tornado 핸들러 통합) > Solution 2 (서버측 프록시) > Solution 3 (팝업) > Solution 4 (Nginx)

## 다음 작업을 위한 컨텍스트

### 즉시 할 일
1. **MJPEG 원격 임베딩 해결** — Solution 1~4 중 선택 후 구현
2. **F3 테스트 실행** — `scripts/test_rules.py` + 다운로드된 3개 영상 (P18.3wc에서 이월)

### 중기 과제
3. Streamlit 이벤트 알림 UI 통합
4. F4 (정체/사고 감지) 구현 착수

### 포크 가이드
- 동일 프로젝트 내 다음 순번: P18.5wc
- MJPEG 이슈 해결 여부에 따라 Streamlit 통합 작업 범위 결정
