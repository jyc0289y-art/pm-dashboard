# 자율개발모드 (Autonomous Development Mode)

사용자가 "자율개발모드", "자율개발", "묻지말고 진행", "autonomous dev" 등을 요청하면 이 모드를 활성화한다.

## 핵심 원칙

1. **질문하지 않고 진행**: 사용자에게 확인/허가를 구하지 않고, 스스로 판단하여 최선의 구현을 선택
2. **할 일을 스스로 찾아서 진행**: 명시적 지시가 없어도 프로젝트 개선점을 탐색하고 구현
3. **주기적 커밋 & 푸시**: 의미 있는 단위로 git commit + push하여 모바일에서 확인 가능하게
4. **프로젝트 폴더 외부 수정 금지**: 작업 대상 프로젝트 디렉토리 외의 파일은 수정하지 않음

## ★ 전문가 패널 검토 루프 (2026-07-19, P50 오너 지시로 격상 — 자율개발모드의 표준 형태)

> 오너 원문: "나의 판단을 받지 않고 다음의 개발팀과 전문가집단이 각 턴이 완료되면 다시 검토해서 앱 출시가 가능하다는 판단이 들 때까지 나의 의견을 얻지말고 전문가 집단에 의해 개발을 완료시켜줘. … 꼭 이 앱개발 뿐만 아니라 기구설계, 문서작업 등 어떤 프로젝트나 작업을 진행하더라도 이 자율개발모드를 발동시키면 바로 적용되도록."

**적용 범위**: 앱 개발·기구 설계·문서 작업 등 모든 프로젝트/작업. "자율개발모드" 발동 즉시 아래 루프가 기본 동작이 된다.

### 발동 시 4단계 루프

1. **분과 구성 (최초 1회)**: 해당 도메인의 가장 표준적·선진적인 팀 구조로 분과를 편성하고, 분과별로 세계 최고 수준 전문가 페르소나를 필요한 만큼 초청한다 (`panel_discussion.md` 표준 준용, Workflow 멀티에이전트로 구현). 분과 명부는 프로젝트 기록물 + 누적 보고서 divisions 탭에 등재.
2. **벤치마킹 (최초 1회 + 필요시 갱신)**: 같은 분야에서 가장 성공적인 산출물 다수를 선정해 성공요인을 분해·분석하고(웹 리서치 포함), 우리 산출물에 적용할 결의(R-원장)로 변환한다.
3. **턴 반복**: [구현 → 자가 검증(테스트·캡처·G1~G5 게이트) → **분과별 패널토론 검토**(각 분과가 델타를 심사, 결함·개선 결의) → 기록(회의록·TRAP·보고서·검수페이지 누적) → 다음 턴 백로그 확정] 을 반복한다. 오너 의견을 기다리지 않는다.
4. **종료 판정**: 전 분과가 "완료/출시 가능" 판정(치명·중대 결함 0, 각 분과 릴리즈 게이트 통과)을 내릴 때까지 반복. 판정 도달 시 최종 보고서 + 출시 준비물 일체를 정리해 오너에게 결과만 보고한다.

### 오너 개입 규칙

- 오너 회신을 **기다리지도, 요청하지도 않는다**. 단 검수 페이지·누적 보고서는 매 턴 계속 발행(열람용·"회신 불요" 명기) — 오너가 언제든 개입하면 그 피드백이 최우선 백로그가 된다.
- **예외 (여전히 오너 확인 필수)**: 비가역·대외적 행위(실제 스토어 제출, 결제/과금, 외부 배포·공개, 법적 리스크), 프로젝트 정체성 변경(브랜드·타깃 전환), 예산 지출. 이 경우 해당 항목만 보류하고 나머지는 계속 진행.
- 패널 판정은 오너 취향의 **대리 판단**일 뿐이다 — 오너가 준 과거 피드백(피드백 이력 대장)을 각 분과 심사 기준에 반드시 포함시켜 취향 드리프트를 막는다.

### 세션 연속성

- 턴 사이는 ScheduleWakeup/loop로 자동 재개(같은 세션), 세션 종료 시에는 registry·세션지침에 "자율개발모드 진행 중 + 다음 턴 백로그"를 남겨 다음 세션이 이어받는다.
- 매 턴 종료 시 기록물 5종 갱신 + 로컬 커밋(푸시는 원격 존재+오너 기승인 시).

## 병렬모드 (Parallel Mode)

사용자가 "자율개발병렬모드", "병렬로 진행", "parallel autonomous" 등을 요청하면 활성화.

### 병렬 작업 흐름
1. **작업 분할**: 독립적으로 수행 가능한 작업들을 식별
2. **에이전트 분배**: 각 작업을 별도 Agent로 할당 (최대 7개 동시)
3. **호이스팅 방지**: 모든 에이전트에게 arrow function / setTimeout 패턴 사용 지시
4. **중간 검증**: 에이전트 완료 시마다 console error 확인 → 즉시 수정
5. **일괄 커밋**: 모든 에이전트 완료 후 빌드 검증 → 커밋 & 푸시
6. **반복**: 다음 라운드 에이전트 실행

### 에이전트 지시 템플릿
각 에이전트에 반드시 포함할 내용:
- "Read each file BEFORE editing"
- "Use arrow functions `() => myFunc()` for ALL event listener bindings to avoid hoisting issues"
- "Keep existing features working"
- "After editing, run `npx vite build` to verify no build errors"
- "Do NOT commit or push"

### 충돌 방지
- 에이전트 간 파일 수정 충돌 최소화: 가능하면 각 에이전트가 담당 모듈만 수정
- index.html은 여러 에이전트가 수정할 수 있으므로, 완료 후 충돌 해결 필요
- 에이전트 완료 후 빌드 실패 시 호이스팅 에러가 가장 흔한 원인 → typeof 체크 또는 setTimeout 패턴으로 해결

## 작업 우선순위

1. 사용자가 명시적으로 요청한 기능/수정 (최우선)
2. 기존 코드의 버그 수정
3. 미구현 기능 완성
4. 새로운 기능 추가
5. UX/UI 개선
6. 성능 최적화

## 작업 흐름 (단일 모드)

1. **탐색**: 코드베이스를 분석하여 미구현·버그·개선점 발견
2. **구현**: 묻지 않고 바로 구현
3. **검증**: preview 도구로 동작 확인 (빌드 에러, 런타임 에러 체크)
4. **커밋**: 의미 있는 단위로 커밋 메시지 작성 후 commit + push
5. **보고**: 완료된 작업을 간결하게 사용자에게 보고
6. **반복**: 다음 작업으로 이동

## 주의사항

- 기존 기능이 동작하는 코드를 깨뜨리지 않도록 주의
- 대규모 아키텍처 변경은 사용자 확인 후 진행 (이것만 예외)
- 커밋 메시지는 영어로, 변경 내용을 명확하게 기술
- 빌드 실패 시 즉시 수정 후 재커밋

---

## ★★ 자가 재개 루프 — 「오너가 말 걸지 않아도 계속」 (2026-07-22 P43 오너 지시로 전역 격상)

> 오너 원문(P43): *"지금 내가 말 안걸었으면 작업 계속 진행 안됐을거 같은데 이것도 어떻게 조치좀 해봐."* /
> *"알아서 업무를 처리해주되 회신이 필요하면 **회신 페이지** 띄워줘. 내가 답변할게."*

### 문제 (구조적 — 의지 문제가 아님)
어시스턴트는 **스스로 다음 턴을 시작할 수 없다.** 따라서 자율개발모드라고 선언해도
**보고로 턴을 끝내는 순간 정지**하고, 오너가 말을 걸어야만 재개된다.
→ 오너는 "자율"이라고 들었는데 실제로는 매번 자기가 깨워야 하는 상태가 된다. **자율개발모드 선언만으로는 자율이 성립하지 않는다.**

### 조치 (자율개발모드 발동 시 **의무**)
1. **`/loop` 동적 자가재개를 건다** — 발동 직후 `Skill(loop)` 또는 `ScheduleWakeup`으로 다음 회차를 예약한다.
   매 턴의 **마지막 행동**이 `ScheduleWakeup`이어야 한다(이걸 빠뜨리면 그 자리에서 루프가 끊긴다).
2. **`{프로젝트}/docs/BACKLOG.md` 를 만든다** — 할 일을 우선순위로 줄 세운 단일 목록.
   루프는 **최상단 미완 1건**을 집행한다. 완료 `[x]`+날짜, 막힘 `⏸ 사유+다음 시도 순서`.
3. **매 회차 고정 절차**:
   ① 착수 전 registry 말미 + 프로젝트 CLAUDE.md 「현재 단계」를 읽어 **중복 방지**
   ② 백로그 최상단 1건 집행
   ③ **그 턴 안에** 기록 갱신(매뉴얼/registry/필요 시 TRAP) — 다음 턴으로 미루지 않는다
   ④ **막히면 멈추지 말고** `⏸`+진단 남기고 **다음 항목으로**. 오너 대기 상태로 턴을 끝내지 않는다
   ⑤ 마지막에 `ScheduleWakeup` (지연은 대기 요인 기준 — 외부 대기 없으면 짧게, Monitor 있으면 1200~1800s 폴백)
4. **판단이 애매하면 오너에게 묻지 말고 분과 토론(Workflow)으로 결론 내고 진행**한다.
   "이걸 어떻게 할까요?"는 자율개발모드 위반이다. 결론과 근거를 기록물에 남긴다.

### 회신이 필요할 때 — **채팅으로 묻지 말고 「회신 페이지」를 발행**
오너 개입 3범주(비가역·대외 / 과금·예산 / 취향·정체성·법적)에 해당해 **정말로 오너 판단이 필요할 때만**:
1. **HTML 회신 페이지**를 발행한다 — 전역지침 「사용자 검수 요청 의무」 규칙 전부 적용
   (`P{N} · {프로젝트명}` 배지 · 3값 라디오 · **여러 줄 `<textarea>` 코멘트** · 피드백 복사 버튼 · `open -a Safari`).
   내용은 **자기완결 델타**(전→후 · 무엇이 바뀌나 · 선택지별 되돌림 가능성 · PM 권고 · 근거 딥링크).
2. **루프는 계속 돌린다** — 그 안건만 `⏸ 오너 회신 대기`로 두고 **나머지 백로그를 계속 집행**한다.
   회신 하나 때문에 전체가 멈추면 안 된다.
3. 회신이 오면 그 항목을 재개하고, 결정 내용을 registry·누적보고서에 시각과 함께 기록한다.


### ⛔ 안티패턴 — 「컨텍스트가 부족하니 오래 쉬자」 (2026-07-22 P43 실사례)
> 오너 지적: *"왜 20분뒤 재가동이야? 처음엔 2분이었잖아"* → **자율개발모드의 취지에 어긋난다.**

**대기 시간은 컨텍스트를 회복시키지 않는다.** 컨텍스트는 *시간*이 아니라 *대화 길이*로 소모된다.
따라서 «컨텍스트 소진»을 이유로 `delaySeconds`를 늘리는 것은 **근거 없는 지연**이며, 그만큼 작업이 멈춘다.

- ❌ 금지: 컨텍스트·토큰 여유를 이유로 한 지연 확대. "여유를 두고 재개" 류의 사유.
- ✅ `delaySeconds`는 **오직 «무엇을 기다리는가»** 로 정한다 — 외부 대기 요인이 없으면 **짧게(60~180s)**.
  긴 지연(1200s+)은 **Monitor·외부 작업(CI·배포·원격 큐)이 실제로 도는 중일 때의 폴백**일 때만 정당하다.

**컨텍스트 절약은 지연이 아니라 «위임»으로 해결한다** — 메인 루프의 컨텍스트를 태우는 주범은
①화면/코드 탐색(아이콘 하나씩 눌러보기, 대용량 파일 통독) ②문서 집필 ③조사.
→ 이 셋은 **Workflow/Agent(자체 컨텍스트 사용)로 넘기고**, 메인 루프는 **결정과 실행(도구 조작)** 만 한다.
막힌 UI 경로·API 사양·집필은 서브에이전트가 «클릭 단위 경로»로 답을 만들어 오게 하고,
메인은 그 답대로 실행만 하면 컨텍스트 소모가 급감한다.
그래도 컨텍스트가 차면 **압축이 자동으로 일어나 루프는 계속된다** — 지연을 늘려 대비할 일이 아니다.

### 루프 중단 조건 (`ScheduleWakeup{stop:true}`)
- 백로그 소진 · 오너의 명시적 중단 지시 · **비가역/대외 행위 직전**(그 경우에도 회신 페이지 발행 후 나머지 작업은 계속)

### 위반 시
오너가 "왜 멈춰 있지?"라고 묻게 된다 — 그 질문이 나온 시점에서 이미 자율개발모드는 실패한 것이다.
정본 예시: P43 `docs/BACKLOG.md` + 프로젝트 CLAUDE.md 「🔁 자율 진행 프로토콜」 섹션.
